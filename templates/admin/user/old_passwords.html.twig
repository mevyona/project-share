{% extends 'base.html.twig' %}

{% block title %}Mots de passe anciens{% endblock %}

{% block body %}
<div class="container mt-4">

    <h1 class="mb-4 text-primary fw-bold">
        🔐 Utilisateurs n’ayant pas changé leur mot de passe depuis {{ days }} jours
    </h1>

    <div class="card shadow-sm">
        <div class="card-header bg-warning text-dark">
            <h5 class="mb-0">Liste des utilisateurs concernés</h5>
        </div>

        <div class="card-body p-0">
            {% if users is empty %}
                <div class="p-4 text-center text-muted">
                    Aucun utilisateur avec un mot de passe trop ancien.
                </div>
            {% else %}
                <table class="table table-striped table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Email</th>
                            <th>Nom</th>
                            <th>Dernier changement</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for u in users %}
                            <tr>
                                <td>{{ u.email }}</td>
                                <td>{{ u.lastname }} {{ u.firstname }}</td>
                                <td>
                                    {% set last = u.passwordHistories|last %}
                                    {% if last %}
                                        {{ last.changedAt|date('d/m/Y à H:i') }}
                                    {% else %}
                                        <span class="text-muted">Inconnu</span>
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
